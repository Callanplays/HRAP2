"""HRAP (HCAT Fork) — Hybrid Rocket Analysis Program (MATLAB-parity Python engine)."""

APP_NAME = "HRAP (HCAT Fork)"
# Single source of truth for app + GitHub Release artifact names (tag as vX.Y.Z).
__version__ = "1.1.0"
